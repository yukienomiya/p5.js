name: Trigger update on p5.js-website

on:
  release:
    types: [published]

jobs:
  ping-p5jswebsite-repo:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: Dispatch repository event to p5.js-website
        run: |
          curl -XPOST https://api.github.com/repos/yukienomiya/p5.js-website/dispatches \
          -H "Accept: application/vnd.github.everest-preview+json" \
          -H "Content-Type: application/json" \
          -u ${{ secrets.WORKFLOW_TESTING }} \
          --data '{"event_type": "ping"}'